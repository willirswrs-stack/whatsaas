# Diário de Bordo - 30/06/2026

Hoje realizamos a implementação e homologação de três grandes marcos de segurança e integração do WhatSaas com as diretivas da Meta.

---

## 🚀 Funcionalidades Desenvolvidas

### ⚙️ 1. WhatsApp Flows (Formulários Nativos)
- **Backend:**
  - Criado o método `sendFlow` na interface [IWhatsAppProvider](file:///c:/Users/Usuario/whatsaas/backend/src/modules/whatsapp/whatsapp-provider.interface.ts).
  - Implementado o adaptador no [evolution.adapter.ts](file:///c:/Users/Usuario/whatsaas/backend/src/modules/whatsapp/adapters/evolution.adapter.ts) conectando ao endpoint `/message/sendInteractive` da Evolution API v2 para disparo de Flows Meta.
  - O motor do fluxo em [flows.service.ts](file:///c:/Users/Usuario/whatsaas/backend/src/modules/flows/flows.service.ts) agora manipula nós de tipo `whatsapp_flow` e coloca a execução em `waiting_response`.
  - Tratado o webhook de retorno (`nativeFlowResponseMessage`) em [evolution-webhook.controller.ts](file:///c:/Users/Usuario/whatsaas/backend/src/modules/evolution/evolution-webhook.controller.ts) para parsear os campos preenchidos, salvá-los no campo `customFields` (JSONB) do contato e retomar a execução.
- **Frontend:**
  - Criado o componente de nó gráfico `WhatsAppFlowNode` e painel de configuração no Canvas do editor de fluxos em [page.tsx](file:///c:/Users/Usuario/whatsaas/frontend/src/app/flows/%5Bid%5D/page.tsx).

### 📥 2. Suporte a Contatos com Número Oculto (@lid e Usernames)
- **Bypass de Validação:** Atualizado o método `resolveAndValidateNumber` no adaptador da Evolution API para aceitar e trafegar JIDs do tipo `@lid` diretamente sem falhar na verificação de formato numérico de telefone.
- **Importação de Membros:** O serviço de contatos [contacts.service.ts](file:///c:/Users/Usuario/whatsaas/backend/src/modules/contacts/contacts.service.ts) foi modificado para não ignorar JIDs de tipo `@lid` na importação de grupos, salvando o identificador completo no campo `phone`.
- **Usernames:** Capturado o atributo `username` fornecido pela Evolution API e gravado dentro de `customFields: { username: '@exemplo' }` do contato.

### 🔀 3. Roteador Híbrido (Official -> Unofficial Switch)
- **Instance Relation:** Mapeada a relação `@ManyToOne` com a entidade `Tenant` no arquivo [instance.entity.ts](file:///c:/Users/Usuario/whatsaas/backend/src/modules/instances/entities/instance.entity.ts).
- **Chaveamento Inteligente:** Em [evolution-webhook.controller.ts](file:///c:/Users/Usuario/whatsaas/backend/src/modules/evolution/evolution-webhook.controller.ts), se a resposta do lead vier em uma instância do tipo `official`, o roteador híbrido busca uma instância ativa `unofficial` do mesmo tenant e altera dinamicamente `execution.instanceId` para ela. As próximas mensagens do fluxo são enviadas pelo chip secundário de forma gratuita.

---

## 📦 Deploy e Validação
- **Repositório Atualizado:** Corrigida a referência da imagem no [docker-compose.prod.yml](file:///c:/Users/Usuario/whatsaas/docker-compose.prod.yml) de `atendai/evolution-api:v2.2.3` (obsoleto/inacessível) para o repositório oficial da comunidade: `evoapicloud/evolution-api:v2.2.3`.
- **Commit & Push:** Código comitado e enviado ao branch `main` do GitHub.
- **Deploy VPS:** O script `.deploy/deploy.js` realizou o empacotamento, upload e a inicialização de todos os containers via Docker compose com sucesso no servidor de produção `2.25.159.205`.
- **Banco de Dados & Health Check:** Migrações, colunas do Asaas e seeds de banco foram aplicadas via `scratch/check_prod.js`. O Health check da API de produção respondeu com sucesso (`status: "ok"`).
